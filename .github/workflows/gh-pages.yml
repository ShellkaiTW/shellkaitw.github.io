name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install pnpm 📦
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: Install Node.js 20 🟢
        uses: actions/setup-node@v4
        with:
          node-version: '20' # 這裡強制鎖定版本，解決 localStorage 報錯問題
          cache: 'pnpm'

      - name: Install dependencies 🔧
        run: pnpm install --frozen-lockfile

      - name: Build 🏗️
        run: pnpm build

      - name: Deploy 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build # Urara/SvelteKit 預設的輸出目錄
          force_orphan: true